<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white p-10">
    <div class="w-full min-h-screen">
        <h3 class="text-3xl mb-5">Edit Your Post</h3>

        <form action="/update/<%= post._id %>" method="post">
            <textarea name="content"
                class="block w-1/3 p-3 rounded-md bg-transparent border-2 border-zinc-800 outline-none resize-none"><%= post.content %></textarea>

            <input type="submit" value="Update Post"
                class="mt-3 px-3 py-2 bg-yellow-500 text-sm rounded-md cursor-pointer text-black font-semibold">
        </form>
    </div>
</body>

</html>