<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white p-10">

    <div class="flex justify-end w-full mb-5">
        <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">Logout</a>
    </div>

    <div class="flex items-start gap-2 mb-5">
        <div class="w-10 h-10 rounded-md overflow-hidden">
            <img class="w-full h-full object-cover" src="/images/uploads/default.png" alt="">
        </div>
        <h3 class="text-3xl tracking-tighter">
            <span class="text-zinc-400 font-light">Hello</span>
            <%= user.name %> 👋
        </h3>
    </div>

    <h5 class="mb-5">You can create a new post.</h5>

    <form action="/post" method="post">
        <textarea placeholder="What's on your mind?" name="content"
            class="block w-1/3 p-3 bg-transparent border-2 border-zinc-800 rounded-md outline-none resize-none text-sm"></textarea>
        <input type="submit" value="Create New Post"
            class="block w-40 px-3 py-2 bg-blue-500 rounded-md mt-2 text-sm cursor-pointer">
    </form>

    <div class="posts-container mt-20">
        <h3 class="text-zinc-400 mb-5">Your Posts</h3>

        <% user.posts.reverse().forEach(function(post){ %>
            <div class="post w-1/3 p-4 border-[1px] border-zinc-700 rounded-md mb-4 bg-zinc-800/50">
                <h4 class="text-blue-500 mb-2">@<%= user.username %>
                </h4>
                <p class="text-sm tracking-tight">
                    <%= post.content %>
                </p>

                <div class="btns flex gap-4 mt-5">
                    <a class="text-blue-500" href="/like/<%= post._id %>">Like</a>
                    <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                </div>
            </div>
            <% }) %>
    </div>

</body>

</html>