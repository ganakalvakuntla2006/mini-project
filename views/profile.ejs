<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white p-10">
    <div class="flex justify-end">
        <a class="bg-red-500 px-3 py-2 rounded-md text-sm" href="/logout">Logout</a>
    </div>

    <h3 class="text-3xl"><span class="font-light">Hello</span>, <%= user.name %> 👋</h3>
    <h5 class="mb-5">you can create a new post.</h5>

    <form action="/post" method="post">
        <textarea placeholder="What's on your mind?" name="content"
            class="block w-1/3 p-3 rounded-md bg-transparent border-2 border-zinc-800 outline-none resize-none"></textarea>
        <input type="submit" value="Create New Post"
            class="mt-3 px-3 py-2 bg-blue-500 text-sm rounded-md cursor-pointer">
    </form>

    <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-1/3 p-4 border-2 border-zinc-800 rounded-md mb-3">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %>
                    </h4>
                    <p class="text-sm tracking-tight">
                        <%= post.content %>
                    </p>

                    <small class="inline-block mt-1 text-zinc-500">
                        <%= (post.likes || []).length %> likes
                    </small>

                    <div class="btns flex gap-4 mt-3">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= (post.likes && post.likes.indexOf(user._id)===-1) ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                    </div>
                </div>
                <% }) %>
        </div>
    </div>
</body>

</html>